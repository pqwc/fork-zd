# pages/test

Окно **тестирования стратегий** — HTTP/TLS/Ping проверки, редактор targets.

## Файлы

| Файл | Роль |
|------|------|
| `test_window.py` | `TestWindow` — вкладки output / favorites / targets |

## Зависимости

- `shared/ui`, `shared/i18n`, `shared/lib`
- `widgets/` — toolbar, codicon buttons, таблицы
- `features/editor/lib` — line editor, highlighters

## Открытие

Через `tools_mixin` главного окна (меню «Тест»).
