# features/winws_setup

Первичная настройка папки winws при отсутствии runtime.

## UI

- `ui/winws_setup_dialog.py` — скачать / выбрать / пропустить

## Вызывается из

- `ZapretDesktop.py` (gate перед MainWindow)
- `updates_mixin` (повторная настройка)
