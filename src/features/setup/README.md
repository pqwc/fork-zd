# features/setup

First-run окно (legacy, **не используется** в текущей версии).

## UI

- `ui/first_run_window.py` — помечено WONTFIX в аудите

Winws setup выполняется через `features/winws_setup/`.
